using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Decoy : MonoBehaviour {

    /*
    public GameObject searchTarget;

    private EnemyBase eBase;
    private const string targetTag = "EnemySearchTarget";

    private void OnTriggerEnter(Collider other) {
        if (other.CompareTag(targetTag) && searchTarget.activeInHierarchy) {
            eBase = other.gameObject.GetComponentInParent<EnemyBase>();
            if (eBase) {
                eBase.Attraction(searchTarget, EnemyBase.AttractionType.PaperPlane, true, 0.6f);
            }
        }
    }
    */

}
